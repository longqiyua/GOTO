<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--
        GOTO 是本地应用搜索与启动器，需要读取用户已安装应用集合来完成
        Engine 的索引、Prethink 候选与真实启动；发布到 Google Play 时需按
        应用搜索/启动器用途申报此权限。
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- GOTO Where Phase 3: Android MVP Integration -->
    <!-- 使用情况访问（需用户在系统设置中授权，非运行时权限） -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- 通知发送（Android 13+ 运行时权限） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 接收通知反馈广播 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppIndex"
        android:hardwareAccelerated="true">

        <!-- ═══ 主前端入口（WebView 加载 GOTO Page） ═══ -->
        <!-- 原 Kotlin UI（UIUX 目录）已归档至 archive/ui-legacy/，保留作纪念 -->
        <activity
            android:name=".webapp.GotoWebActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.AppIndex.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 桌面悬浮条搜索 Service（免费功能） -->
        <service
            android:name=".QuickActionsAndFloatingWindow.OverlaySearchService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- GOTO Where Service（Phase 3 Android MVP） -->
        <service
            android:name=".where.WhereService"
            android:exported="false"
            android:label="GOTO Where" />

        <!-- GOTO Where 通知反馈接收器 -->
        <receiver
            android:name=".where.WhereFeedbackReceiver"
            android:exported="false" />

        <!-- V2.1: 应用变更监听（静态注册，常驻不依赖 ViewModel） -->
        <receiver android:name="com.appindex.AppRegistry.PackageReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <action android:name="android.intent.action.PACKAGE_CHANGED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
